# How to Run the Project

Follow the steps below to train the model and generate predictions.

---

## 1. Train the Model

Run the training command to perform hyperparameter tuning and train all models
on the full dataset:

```bash
python3 spam_classifier.py train
```
---

## 1. Generate Predictions

```bash
python3 spam_classifier.py predict
```